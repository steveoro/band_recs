<!-- Main menu section: -->
<%= render( :partial => AGEX_APP + '/main_menu') %>

<h1><%= t(:recordings, {:scope=>[:agex_action]}) %></h1>


<%= netzke :recording_display_panel, {
	:class_name => "RecordingDisplayPanel",
	:title => t(:recordings, {:scope=>[:agex_action]}),
	:record_id => @recording_id,
	:width => "98%",
	:height => 500,
	:border => true,
	:view_config => {
		:force_fit => true # force the columns to occupy all the available width
	}
} %>
