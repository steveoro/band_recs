<!-- Main menu section: -->
<%= render( :partial => AGEX_APP + '/main_menu') %>

<h1><%= t(:authors, {:scope=>[:agex_action]}) %></h1>

<%= netzke :author_management_panel, {
	:class_name => "AuthorManagePanel",
	:title => t(:authors, {:scope=>[:agex_action]}),
	:width => "98%",
	:height => 500,
	:border => true,
	:view_config => {
		:force_fit => true # force the columns to occupy all the available width
	}
} %>
